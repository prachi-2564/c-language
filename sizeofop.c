#include<stdio.h>
void main()
{
	int a;
	float f;
	double d;
	char ch;
	printf("\n size of integer=%d bytes",sizeof(a));
		printf("\n size of float=%d bytes",sizeof(f));
			printf(" \n size of double=%d bytes",sizeof(d));
				printf("\n size of character=%d bytes",sizeof(ch));
}